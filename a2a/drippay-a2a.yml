generated: '2026-08-13'
method: probed
source: https://docs.usedrip.ai/.well-known/agent-card.json
card_file: a2a/drippay-agent-card.json
discovery:
  path: /.well-known/agent-card.json
  canonical: true
  host: docs.usedrip.ai
  note: >-
    Served from the provider's own documentation host. The response header
    x-matched-path is /_sites/[subdomain]/.well-known/agent-card.json, so the
    card is emitted by the Mintlify docs platform on Drippay's behalf rather
    than hand-authored — it is still published by the provider from a host the
    provider controls. The apex marketing host (dreach.ai, formerly usedrip.ai)
    and the API host (api.drippay.dev) both return 404 for both card paths.
x-evidence:
  fetched: '2026-08-13'
  url: https://docs.usedrip.ai/.well-known/agent-card.json
  http_status: 200
  content_type: application/json
  bytes: 955
card:
  name: dripos
  url: https://docs.usedrip.ai/
  version: 1.0.0
  protocolVersion: '0.3'
  preferredTransport: HTTP+JSON
  provider_organization: dripos
  documentationUrl: https://docs.usedrip.ai/
  capabilities:
    streaming: false
    pushNotifications: false
  skills:
  - id: drip
    name: Drip
    url: https://docs.usedrip.ai/.well-known/agent-skills/drip/skill.md
grade:
  level: conformant
  against: A2A 1.0.0
  hard_checks:
    capabilities_is_object: true
    protocol_version_present: true
    skills_is_array: true
  optional_checks:
    preferred_transport_present: true
    default_input_modes_present: true
    default_output_modes_present: true
deviations:
- id: protocol-version-behind
  detail: >-
    protocolVersion is "0.3", not "1.0.0" — the card declares the pre-1.0 A2A
    protocol revision.
- id: url-points-at-docs-not-an-agent-endpoint
  detail: >-
    Both `url` and every entry in `supportedInterfaces` point at the docs site
    root (https://docs.usedrip.ai/) rather than an A2A JSON-RPC endpoint, so the
    card is discoverable but not directly callable as an A2A agent.
- id: skill-tags-empty
  detail: skills[0].tags is an empty array, so the skill carries no routing hints.
- id: no-a2a-transport-for-the-billing-api
  detail: >-
    The card describes the dripos Mac-app assistant surface only. The Drip
    billing API (api.drippay.dev) and the hosted MCP server
    (https://mcp.drippay.dev/mcp) are not referenced by the card.
related:
  agent_skill: skills/drippay-drip-published.md
  mcp: mcp/drippay-mcp.yml
